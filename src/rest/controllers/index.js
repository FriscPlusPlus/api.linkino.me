const contact = require('./contact.controller');
const links = require('./links.controller');
const clips = require('./clips.controller');
const auth = require('./auth.controller');

module.exports = {
  contact,
  links,
  clips,
  auth
};
